## Packages
framer-motion | Page transitions and animations
recharts | Dashboard progress charts
clsx | Class name merging
tailwind-merge | Class name merging

## Notes
All API endpoints use /api prefix and handle Zod validation.
Assuming cookies are used for auth via credentials: "include".
